#include<stdio.h>
#include<conio.h>
int main()
{
clrscr();
char a [20]="samad";  /*We use %s for string and %c for char but
		       both are declared as (char name[x] = "anything";)*/
printf("%s",a);
getche();
return 0;
}
